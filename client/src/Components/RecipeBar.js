import React from "react";
import styled from "styled-components";
const RecipeBar = ({}) => {
  return <Div>RecipeBar</Div>;
};

const Div = styled.div`
  display: flex;

  width: 100%;
  height: 100%;
`;

export default RecipeBar;
